age = 12
if age < 4:
    price = 0
elif age < 18:
    price = 25
else:
    price = 40

print(f"Your admission cost is ${price}.")

# you can use multiple elif
# if you want one block of code needs to run, use an 'if-elif-else' chain. if more than one block of code needs to run, use a series of independent 'if'stmnts.
