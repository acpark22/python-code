def show_messages(texts):
    """Print a series of text messages."""
    for text in texts:
        msg = f"{text}"
        print(msg)
greetings = ['Be right back.', 'Hold it', 'Hello there']
show_messages(greetings)

